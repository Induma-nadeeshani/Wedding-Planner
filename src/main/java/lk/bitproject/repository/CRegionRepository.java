package lk.bitproject.repository;

import lk.bitproject.model.CRegion;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CRegionRepository extends JpaRepository<CRegion, Integer> {
}
