package lk.bitproject.repository;

import lk.bitproject.model.VModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VModelRepository extends JpaRepository<VModel, Integer> {
}
