package lk.bitproject.repository;

import lk.bitproject.model.SpStatus;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SpStatusRepository extends JpaRepository<SpStatus, Integer> {
}
