package lk.bitproject.repository;

import lk.bitproject.model.DStatus;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DStatusRepository extends JpaRepository<DStatus, Integer> {
}