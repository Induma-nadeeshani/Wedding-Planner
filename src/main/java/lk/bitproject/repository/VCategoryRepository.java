package lk.bitproject.repository;

import lk.bitproject.model.VCategory;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VCategoryRepository extends JpaRepository<VCategory, Integer> {
}
