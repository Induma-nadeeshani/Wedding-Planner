package lk.bitproject.repository;

import lk.bitproject.model.VStatus;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VStatusRepository extends JpaRepository<VStatus, Integer> {
}
